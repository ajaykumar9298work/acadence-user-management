spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://db.fdenipvnbiahbwxigsmd.supabase.co:5432/postgres
    username: postgres
    password: z.p4a8vYr6x5LJx
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

# Enable or disable authentication
security:
  authentication-enabled: true
